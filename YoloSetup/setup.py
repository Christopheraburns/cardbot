from distutils.core import setup

setup(name='yolo',
      version='0.1',
      author='christopheraburns@gmail.com',
      description='install yolo playing card recognition',
      py_modules= [],
      packages=[],
      package_data={'': ['libdarknet.so']})